import * as underTest from './price-range-control.component';

describe('price-range-control.component', () => {
  it('exports a module surface', () => {
    expect(underTest).toBeTruthy();
  });
});
