import * as underTest from './session-timeout-modal.component';

describe('session-timeout-modal.component', () => {
  it('exports a module surface', () => {
    expect(underTest).toBeTruthy();
  });
});
