import * as underTest from './payment-method-selector.component';

describe('payment-method-selector.component', () => {
  it('exports a module surface', () => {
    expect(underTest).toBeTruthy();
  });
});
