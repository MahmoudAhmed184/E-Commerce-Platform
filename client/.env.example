# Angular currently uses compile-time files in src/environments/.
# Keep this file as the frontend environment template for local setup.
# It is not loaded by ng serve automatically until a runtime env loader is added.
NG_APP_API_BASE_URL=http://127.0.0.1:8000/api/v1
